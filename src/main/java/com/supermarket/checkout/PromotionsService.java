package com.supermarket.checkout;

import java.util.Map;

public class PromotionsService {

/*    public int computePriceDiffFromMultiPricePromotion(Map<Item, Integer> productsEligibleForPromotions, Product2 product) {
        int qty = productsEligibleForPromotions.getOrDefault(product, 0);
        int q = qty/ 2;
        int r = qty % 2;
        int priceDiff = - (product.getPrice() * qty) + q * 125 + r * product.getPrice();
        return priceDiff;
    }*/
}
